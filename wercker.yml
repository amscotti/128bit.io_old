box: wercker/ubuntu12.04-ruby2.0.0
build:
  steps:
    - bundle-install
    - script:
        name: jekyll build 
        code: bundle exec jekyll build
deploy:
    steps:
        - bundle-install
        - script:
            name: Deploy to S3
            code: bundle exec s3_website push
